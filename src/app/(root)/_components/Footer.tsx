import { mainPaddingX } from '@/constants/common';

export default function Footer() {
  return (
    <footer
      className={`w-full min-h-[310px] bg-[#111111] px-[${mainPaddingX}px]`}
    >
      <div></div>
    </footer>
  );
}
